{% extends "base.twig" %}

{% block title %}Search results{% endblock %}

{% block content %}
    <h2>Search Results</h2>
    <div>
        {% for video in result %}
            {{ video.subject }}
        {% endfor %}
    </div>

{#
    {{ dump (result) }}
    {% for video in result %}
        {{ video }}
        {% endfor %}
#}
    {{ error }}


{% endblock %}